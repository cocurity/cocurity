# -- Vercel Postgres (auto-injected by Vercel when linked) --
# For local dev with docker compose: use the values below as-is
POSTGRES_PRISMA_URL="postgresql://cocurity:cocurity@localhost:5432/cocurity"
POSTGRES_URL_NON_POOLING="postgresql://cocurity:cocurity@localhost:5432/cocurity"

# -- Vercel Blob (auto-injected by Vercel when linked) --
BLOB_READ_WRITE_TOKEN=""

# -- App --
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

NEXT_PUBLIC_AGENT_ROLE=both
NEXT_PUBLIC_FF_MOCK=0
NEXT_PUBLIC_FF_FIX_ENABLED=0
NEXT_PUBLIC_FF_CERT_ENABLED=0

FF_AI_SUMMARY_ENABLED=0
FF_FIX_ENABLED=0
FF_CERT_ENABLED=0
GITHUB_TOKEN=
